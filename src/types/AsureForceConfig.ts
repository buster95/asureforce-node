type AsureForceConnectConfig = {
  baseURL: string;
  user: string;
  pass: string;
};

// type Sizes = "md" | "lg"
// type Variants = 1 | 2 | 3;
// type V = `${Sizes}-${Variants}`
// const mine: V = ""

export { AsureForceConnectConfig };
